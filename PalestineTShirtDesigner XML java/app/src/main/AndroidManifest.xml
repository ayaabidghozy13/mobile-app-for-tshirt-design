<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PalestineTShirtDesigner"
        tools:targetApi="31">
        <activity
            android:name=".Interface4Activity"
            android:exported="false"
            android:label="@string/title_activity_interface4"
            android:theme="@style/Theme.PalestineTShirtDesigner" />
        <activity
            android:name=".Interface5Activity"
            android:exported="false" />

        <activity
            android:name=".Interface3Activity"
            android:exported="false" />
        <activity
            android:name=".Interface2Activity"
            android:exported="false" />


        <activity
            android:name=".Interface1Activity"
            android:exported="true"> <!-- Marquer l'activité comme exportée -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- Intent principal -->
                <category android:name="android.intent.category.LAUNCHER" /> <!-- Intent de lancement -->
            </intent-filter>
        </activity>
    </application>

</manifest>
